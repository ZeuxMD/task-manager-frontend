import AppLayout from "@/components/layout/app-layout";
import ProfileClient from "@/components/profile/ProfileClient"; // New Client Component

export default function ProfilePage() {
  return (
    <AppLayout>
      <ProfileClient />
    </AppLayout>
  );
}
